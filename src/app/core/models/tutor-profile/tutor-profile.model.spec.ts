import { TutorProfileModel } from './tutor-profile.model';

describe('TutorProfileModel', () => {
  it('should create an instance', () => {
    expect(new TutorProfileModel()).toBeTruthy();
  });
});
