import { UpdateFlaggedPost } from './update-flagged-post.model';

describe('UpdateFlaggedPost', () => {
  it('should create an instance', () => {
    expect(new UpdateFlaggedPost()).toBeTruthy();
  });
});
